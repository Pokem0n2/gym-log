services:
  gym-log:
    build: .
    ports:
      - "1118:1118"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      - ADDR=:1118
      - DB_PATH=/data/gym.db
